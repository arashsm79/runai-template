[project]
name = "runai-template"
version = "0.1.0"
description = "Simple RunAI template"
readme = "README.md"
authors = [
    { name = "Arash Sal Moslehian", email = "arashsm79@yahoo.com" }
]
requires-python = ">=3.11"
dependencies = [
    "tqdm>=4.70.0",
]

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"
